export default {
  created(el, binding, vnode, prevVnode) {
    const bindingValue = binding.value
    if(bindingValue){
      
    }
  }, // 新增
  beforeMount() {},
  mounted() {},
  beforeUpdate() {}, // 新增
  updated() {},
  beforeUnmount() {}, // 新增
  unmounted() {},
}
